import {render, screen} from "@testing-library/react"
import Header from "../Header";


describe("Header Components", ()=>{
    test("clickable profile image", ()=>{
        render(<Header />);
    
        // screen.
        // expect().toB;
    });
});

